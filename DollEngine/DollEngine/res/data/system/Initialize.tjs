
var dm = print;
var em = throwMsg;

var DEConfig = Scripts.jsonStorage("Config.json");

{
    var count = DEConfig.autoPaths.count;
    for (var i = 0; i < count; i++) {
        var file = DEConfig.autoPaths[i];
        dm("LOAD %s",file);
        Scripts.execStorage(file);
    };
}

var de = new Main();