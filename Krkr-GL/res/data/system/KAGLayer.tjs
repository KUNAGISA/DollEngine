class KAGLayer extends Layer
{
	function KAGLayer (win,par) 
	{
		super.Layer(...);
	}

	function setSizeToParentSize (percX, percY) 
	{
		if (parent)
		{
			setSize(parent.width*percX, parent.height*percY);
		}
		else
		{
			setSize(window.innerWidth*percX, window.innerHeight*percY);
		}
	}
}